[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for any route so client-side routing
# (React Router deep links + refresh) works instead of returning 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
