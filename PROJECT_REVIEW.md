# مراجعة شاملة لمشروع MSG Frontend

> تاريخ المراجعة: 2026-09-14  
> النطاق: الكود الحالي، الإعدادات، الوثائق، التاريخ القريب لـ Git، والتعديلات غير الملتزمة.  
> هذه الوثيقة توثق الملاحظات فقط ولا تمثل تنفيذًا للإصلاحات.

## 1. الخلاصة التنفيذية

المشروع عبارة عن **React Prototype** منظم لتدفق تسجيل مندوبي MSG Logistics، ويتضمن بوابات للمندوب والمشرف وHR وداشبورد للمندوب بعد التفعيل.

المشروع قابل للتجربة والعرض محليًا، لكنه **غير جاهز للإنتاج** للأسباب التالية:

- لا توجد مصادقة حقيقية أو جلسة مستخدم.
- لا توجد حماية للمسارات أو صلاحيات حسب الدور.
- لا توجد طبقة API؛ البيانات محلية وMock.
- رفع المستندات غير فعلي.
- انتقالات حالة الطلبات يملكها العميل بدل الخادم.
- بيانات الطلبات ومسودة التسجيل تضيع عند تحديث الصفحة.
- الترجمة غير مكتملة، خصوصًا في بوابتي المشرف وHR.
- لا توجد اختبارات آلية.
- توجد بيانات شخصية وهمية وروابط وإعدادات تحتاج مراجعة أمنية.

### التقييم العام

| المجال | التقييم |
| --- | --- |
| تنظيم المشروع | جيد بالنسبة إلى Prototype |
| جودة الواجهة | متوسطة إلى جيدة |
| اكتمال الميزات | متوسط |
| الأمان | غير كافٍ قبل ربط بيانات حقيقية |
| جاهزية الباك إند | غير جاهز |
| الاختبارات | غير موجودة |
| دقة الوثائق | متخلفة عن الكود الحالي |

**الحكم النهائي:** صالح للعرض والتجربة الداخلية، وليس صالحًا للتشغيل ببيانات فعلية أو نشر إنتاجي قبل معالجة البنود الحرجة.

---

## 2. وصف المشروع والتدفق الحالي

التدفق المقصود:

```text
تسجيل المندوب
    ↓
مراجعة المشرف
    ↓
مراجعة HR وإدخال السند
    ↓
قرار أبشر
    ↓
توقيع العقد
    ↓
داشبورد المندوب
```

الرحلة الحالية في الواجهة:

```text
/login
  ├── مندوب → /register/* أو /status
  ├── مشرف → /supervisor/requests
  └── HR → /hr/requests
```

يوجد أيضًا:

- `/dashboard`
- `/profile`
- `/operations`
- `/placeholder/attendance` لصفحة حضور حقيقية
- صفحات Placeholder للتعويض والسلفة والحوادث وغيرها

---

## 3. التقنية والهيكل

### التقنيات

- React 18.3.1
- React DOM 18.3.1
- React Router DOM 7.18.3
- Vite 6.0.5
- ESLint 9 مع React plugins
- Prettier 3.9.6
- JavaScript/JSX بدون TypeScript

### هيكل `src/` الحالي

```text
src/
├── App.jsx
├── main.jsx
├── components/
│   ├── shared/
│   │   ├── AppHeader.jsx
│   │   ├── BottomNav.jsx
│   │   ├── CompanyWarehouseSelect.jsx
│   │   ├── ContractViewer.jsx
│   │   ├── CourierApplicationInfo.jsx
│   │   ├── FormActions.jsx
│   │   ├── GreetingCard.jsx
│   │   ├── ProfileMenu.jsx
│   │   ├── QuickActionsGrid.jsx
│   │   ├── RequestListItem.jsx
│   │   ├── SupervisorCard.jsx
│   │   └── VehicleCard.jsx
│   └── ui/
│       ├── BrandLogo.jsx
│       ├── Icon.jsx
│       ├── LanguageButton.jsx
│       └── QuickActionButton.jsx
├── constants/
│   ├── companies.js
│   ├── mockDashboard.js
│   ├── requestStatus.js
│   ├── routes.js
│   ├── supervisors.js
│   └── contracts/
├── context/
│   └── OnboardingContext.jsx
├── hooks/
│   └── .gitkeep
├── i18n/
│   ├── copy.js
│   └── LanguageContext.jsx
├── layouts/
│   └── OnboardingLayout.jsx
├── pages/
├── services/
│   └── .gitkeep
├── styles/
└── utils/
```

### مجلدات أو مكونات مذكورة في الوثائق وغير موجودة

- `RoleGate`
- `RequestsContext`
- `StatusBadge`
- `ConfirmDialog`
- `RequestsTable`
- `mockRequests.js`
- `src/assets/`
- `useAuth`
- ملفات API داخل `src/services/`

يوجد حاليًا Context واحد يجمع مسؤوليات كثيرة، ولا توجد طبقة خدمات أو Hooks مخصصة.

---

## 4. الملاحظات الحرجة

### C1. المصادقة وهمية

`Login` لا يستدعي API ولا يتحقق من اسم المستخدم أو كلمة المرور. أي قيم غير فارغة تنجح، ويتم تحديد الدور من النص المكتوب أو اختيار محلي.

المخاطر:

- أي مستخدم يستطيع الدخول لأي دور.
- لا توجد هوية موثوقة للمستخدم.
- لا توجد جلسة أو Token أو تجديد جلسة.
- كلمة المرور لا تُستخدم فعليًا.

### C2. جميع المسارات تقريبًا عامة

المسارات التالية يمكن فتحها مباشرة دون تسجيل دخول أو صلاحية:

- `/supervisor/requests`
- `/supervisor/requests/:id`
- `/hr/requests`
- `/hr/requests/:id`
- `/dashboard`
- `/profile`
- `/operations`
- `/status`

لا يوجد `AuthProvider` أو `RoleGate` أو حماية على مستوى routing.

### C3. لا توجد طبقة API

مجلد `src/services/` فارغ. البيانات التالية محلية أو وهمية:

- الطلبات
- بيانات المندوب
- بيانات الداشبورد
- الشركات والمخازن
- المشرفون
- الحضور
- الطلبات التشغيلية
- العقد

### C4. رفع المستندات غير فعلي

صفحة Documents تحفظ قيمة Boolean مثل `true` عند اختيار الملف، لكنها لا تحفظ كائن `File` ولا ترسله إلى الخادم.

لا يوجد:

- تحقق من الحجم.
- تحقق موثوق من MIME/content.
- رفع multipart.
- حالة تقدم الرفع.
- فحص برمجيات خبيثة.
- معرف مستند من الخادم.

### C5. الواجهة تملك حالة الطلبات

`updateRequest(id, updates)` يقبل أي تغييرات مباشرة ولا يفرض انتقالات صحيحة للحالات.

هذا يسمح نظريًا بانتقال الطلب من أي حالة إلى أي حالة دون تحقق. يجب أن يكون الخادم هو المصدر النهائي للحالة وأن يفرض state machine والتحقق من التعارض.

### C6. بيانات شخصية وهمية داخل التطبيق

`OnboardingContext` يحتوي على أسماء وهويات وهواتف وأرقام IBAN وبيانات طلبات كاملة داخل الكود. يجب عدم شحن هذه البيانات إلى الإنتاج حتى لو كانت تجريبية.

### C7. كلمة المرور محفوظة في Context

كلمة المرور موجودة في `personal.password` داخل React state. حتى لو لم تُحفظ في localStorage، لا ينبغي إبقاؤها أكثر من اللازم أو تمريرها ضمن حالة عامة مشتركة.

---

## 5. ملاحظات أمنية

### 5.1 مفتاح Google API في تاريخ Git

تم العثور على مفتاح Google API سابقًا في `.vscode/mcp.json` ضمن commit `f0c0005`.

حذف الملف وإضافة `.vscode/` إلى `.gitignore` لا يلغي المفتاح من تاريخ Git.

الإجراء المطلوب:

1. إلغاء المفتاح فورًا من Google Cloud.
2. إنشاء مفتاح بديل إن كان مطلوبًا.
3. مراجعة تاريخ Git وإعادة كتابته عند الحاجة بالتنسيق مع مالكي المستودع.
4. عدم وضع أسرار في ملفات المشروع أو `VITE_*`.

### 5.2 رابط السند غير موثوق

في `HrRequestDetail` يتم استخدام قيمة `sanadUrl` داخل `href`. التحقق HTML من `type="url"` لا يضمن أن الرابط HTTP/HTTPS ولا يفرض نطاقًا موثوقًا.

المطلوب:

- تحليل الرابط بـ `new URL`.
- السماح بـ `https:` فقط.
- استخدام قائمة نطاقات معتمدة.
- إعادة التحقق من الخادم.
- عدم اعتبار `rel="noreferrer"` حماية من الرابط الخبيث.

### 5.3 الاعتماد على العميل للصلاحيات

أي role أو status أو company ID أو warehouse ID أو document ID يأتي من المتصفح يعتبر غير موثوق. يجب أن يتحقق الخادم من الملكية والصلاحية والحالة الحالية في كل عملية.

### 5.4 بيانات حساسة معروضة كاملة

الهوية والآيبان وبيانات السند تظهر في بعض الصفحات أو الـ mocks بشكل كامل. يفضل إخفاء أجزاء منها حسب الدور، وعدم عرضها إلا لمن يملك صلاحية.

### 5.5 `fillTemplate`

الاستخدام الحالي آمن لأنه يعرض الناتج كنص React وليس عبر `dangerouslySetInnerHTML`. يجب المحافظة على ذلك وعدم استخدام HTML خام مستقبلًا دون sanitization موثوق.

---

## 6. مراجعة الـ Routing

### المشاكل

- مسارات تفاصيل الطلب مكتوبة يدويًا في:
  - `SupervisorRequests/index.jsx`
  - `HrRequests/index.jsx`
- لا توجد دوال مثل `supervisorRequestDetail(id)` و`hrRequestDetail(id)`.
- مسارات children داخل `App.jsx` مكتوبة كـ `personal` و`vehicle-bank` و`documents` بدل اشتقاقها من الثوابت.
- الحضور الحقيقي يستخدم `/placeholder/attendance`.
- `PlaceholderPage` ما زال يدرج الحضور ضمن الصفحات القادمة رغم أن الصفحة حقيقية.
- الـ catch-all يعيد المستخدم إلى Login بصمت بدل صفحة 404.
- يمكن فتح أي خطوة من خطوات التسجيل مباشرة دون التأكد من إكمال الخطوة السابقة.
- لا توجد حماية role-based أو session-based.

### التوصية

- إضافة route builders للمسارات ذات المعاملات.
- إنشاء مسار واضح `/attendance`.
- إضافة صفحة 404.
- إضافة حراس Auth وRole.
- تحديد سياسة الوصول المباشر إلى خطوات التسجيل.

---

## 7. مراجعة الحالة وإدارة البيانات

### `OnboardingContext`

يجمع في ملف واحد:

- بيانات تسجيل المندوب.
- السيارة والبنك.
- المستندات.
- قائمة الطلبات.
- الطلب الحالي.
- تحديث الطلبات.
- إنشاء طلب جديد.

هذا يجعل Context واسعًا وصعب الصيانة، ويخلط state مؤقتة مع بيانات workflow مشتركة.

### مشاكل الحالة

- التسجيل والطلبات تضيع عند refresh.
- قرارات المشرف وHR تضيع عند refresh.
- لا يوجد تخزين دائم أو مزامنة مع الخادم.
- لا يوجد تحقق من انتقالات الحالات.
- رقم الطلب العشوائي قد يتصادم.
- fallback data وهمية تستخدم عند نقص البيانات.
- قيمة Context ودواله يعاد إنشاؤها مع كل render.
- يوجد تحذير `react-refresh/only-export-components` في ملفات Context.

### التوصية المعمارية

الفصل إلى:

- `AuthContext`
- `OnboardingContext` للمسودة فقط
- `RequestsContext` أو cache مخصص للطلبات
- `services/` لكل resource
- الخادم كمصدر الحقيقة للحالة والصلاحيات

---

## 8. مراجعة الصفحات

### Login

- دخول وهمي.
- الدور يستنتج من username أو اختيار محلي.
- مؤقت 350ms بلا cleanup.
- تعديل username بعد اكتشاف دور مشرف/HR لا يعيد الدور إلى مندوب.
- Checkbox تذكرني غير مربوط بحالة.
- زر نسيت كلمة المرور بلا تنفيذ.
- لا رسائل خطأ أو loading/error حقيقية.

### PersonalInfo

- البيانات تحفظ في Context فقط.
- التحقق يقتصر غالبًا على `required` وتطابق كلمة المرور.
- لا تحقق من رقم الهوية السعودية أو checksum.
- لا تحقق من صيغة الجوال أو بدايته بـ 5.
- لا تحقق من العمر أو تاريخ الميلاد.
- لا تحقق من الجواز.
- لا يوجد حد مناسب لقوة كلمة المرور.
- زر الحفظ يتجاوز HTML validation لأنه `type="button"`.
- قيمة الجنسية تختلف حسب اللغة، ما قد يجعل القيمة المحفوظة غير مستقرة.
- المدن ثابتة ومحدودة وبعضها إنجليزي في الواجهة العربية.
- placeholder `passwordPh` غير موجود في الترجمة.

### VehicleBank

- البنوك وأنواع المركبات ثابتة ومحدودة.
- لوحة المركبة تقبل أي أحرف تقريبًا.
- تحويل الأرقام لا يضمن إزالة الأحرف غير المطلوبة.
- الآيبان لا يمر عبر تحقق طول/checksum.
- الحقول تظهر عندما `hasVehicle` يساوي `null` بسبب شرط `!== false`.
- زر الحفظ لا يفرض validation.
- يمكن فتح الصفحة مباشرة دون إكمال PersonalInfo.

### Documents

- لا يتم الاحتفاظ بالملف نفسه.
- `accept` مجرد تلميح للمتصفح ويمكن تجاوزه.
- لا تحقق من الحجم أو المحتوى.
- لا preview أو progress أو retry.
- لا توجد API.
- بعض شروط المستندات تعتمد على `hasVehicle !== false` وتشمل حالة `null`.

### CourierStatus

- يعتمد على بيانات Mock وطلب افتراضي ثابت.
- يوجد محدد تجريبي للطلبات عند تعددها.
- بعض النصوص متحرقة بالعربية.
- عرض مراحل الحالة لا يمثل جميع الحالات بوضوح، خصوصًا `ACTIVE`.
- توقيع العقد ينتقل مباشرة إلى الداشبورد فلا تظهر نتيجة التوقيع جيدًا.
- لا حماية من الوصول العام.

### SupervisorRequests

- يعرض كل الطلبات بدل تصفية الطلبات المخصصة للمشرف أو الحالة المطلوبة.
- مسار التفاصيل hard-coded.
- معظم النصوص عربية خارج i18n.
- يعرض status الخام مثل `pending_supervisor`.
- لا يوجد بحث أو filter أو pagination أو empty state مناسب.
- رابط الانتقال إلى HR يمثل shortcut تجريبيًا لا صلاحية فعلية.

### SupervisorRequestDetail

- `alert` و`window.confirm` بدل مكونات UI مترجمة.
- لا loading/error state.
- لا state machine حقيقية.
- رسالة النجاح خضراء حتى بعد الرفض.
- لا يعاد تحميل الطلب عند تغير `id` في نفس المكون.
- لا يوجد سجل تدقيق.

### HrRequests

- يعرض كل الطلبات دون تصفية حسب حالة HR أو أبشر.
- الصفحة شبه عربية بالكامل.
- statuses تعرض كنصوص خام.
- يسمح بإدارة السند حتى للطلبات غير المناسبة.
- لا empty/search/pagination.

### HrRequestDetail

- لا يستخدم `t` تقريبًا.
- الصفحة عربية بالكامل.
- حفظ السند وإرساله لأبشر مدمجان خلاف الخطة.
- رابط السند غير موثوق.
- `alert`/`confirm` غير مترجمين.
- رسالة النجاح تظهر خضراء بعد الرفض أيضًا.

### CourierDashboard

- يعتمد على `mockDashboard`.
- اسم المندوب ثابت.
- `hasVehicle` يأتي من حالة التسجيل، لا من الطلب الحالي/بيانات الخادم.
- كرت المركبة قد يختفي بعد الدخول.
- الإجراءات السريعة تذهب لصفحات Placeholder رغم وجود Operations.
- قائمة الطلبات مختلفة عن قائمة الطلبات في Operations.
- بيانات الطلبات الأخيرة لا تتبدل ترجمتها بشكل كامل.

### UserProfile

- يعتمد على fallbacks وهمية كثيرة.
- يعرض الحساب فعالًا والعقد موقّعًا حتى لو الحالة غير فعالة.
- يفترض وجود مركبة عند نقص البيانات.
- يعرض بيانات شركة/آيبان/سند وهمية.
- Logout مجرد انتقال إلى Login ولا يمسح جلسة أو بيانات.

### Operations

- الطلبات التشغيلية في state محلي.
- الملفات تحفظ كأسماء فقط.
- مؤقت رسالة النجاح بلا cleanup.
- لا تحقق من مدى التواريخ أو بعض الحقول الشرطية.
- بعض النصوص والتلميحات عربية فقط.
- البحث لا يشمل دائمًا `titleEn` و`notesEn`.
- معرفات الطلبات مبنية على طول القائمة وقد تتصادم.
- المودال لا يدعم Escape أو focus trap.
- النقر على الخلفية قد يلغي الحالة دون تأكيد.

### Attendance

- التخزين في `localStorage` مشترك على مستوى المتصفح وليس المستخدم.
- استخدام `toISOString()` يسبب خطأ منطقة زمنية في السعودية/مصر قرب منتصف الليل.
- قراءة التخزين تتم مرتين عند البداية.
- لا API ولا مزامنة مع المستخدم.
- بعد التسجيل يصبح اليوم مقفلًا دون مسار تصحيح واضح.
- المسار ما زال تحت `/placeholder/`.

### PlaceholderPage

- بعض الميزات الحقيقية ما زالت ممثلة كـ coming soon.
- الحضور مذكور كـ placeholder رغم وجود صفحة حقيقية.
- لا توجد سياسة واضحة لفصل placeholder عن الميزات المنفذة.

---

## 9. الترجمة وRTL

### مفاتيح مستخدمة وغير موجودة في `copy.js`

- `passwordPh`
- `notifications`
- `supervisorRequests`
- `supervisorRequestsHint`
- `viewDetails`
- `selectCompany`
- `chooseCompany`
- `selectWarehouse`
- `chooseWarehouse`
- `selectCompanyFirst`

بعضها ينتج نصًا فارغًا، وبعضها يستخدم fallback عربي، لذلك تتعطل تجربة الإنجليزية.

### نصوص متحرقة خارج i18n

- بوابات المشرف وHR تقريبًا بالكامل.
- `alert` و`confirm`.
- قائمة عمليات كاملة داخل `Operations`.
- أسباب الغياب داخل `Attendance`.
- عناوين `PlaceholderPage`.
- أسماء المدن والبنوك وأنواع المركبات.
- أجزاء من `CourierStatus`.
- fallback data في الداشبورد والبروفايل.

### ملاحظات إضافية

- بعض مفاتيح `copy.js` موجودة لكنها غير مستخدمة.
- `GreetingCard` يستخدم فاصلة عربية حتى عند الإنجليزية.
- القيم التي تحفظ حسب اللغة يجب استبدالها بمعرفات ثابتة، مثل `saudi` بدل `سعودي` و`Saudi`.

---

## 10. الوصول Accessibility

النواقص الملحوظة:

- لا `aria-live` عند تغير خطوة التسجيل.
- لا `aria-describedby` لمربوطات المساعدة.
- مؤشر التقدم لا يملك `role="progressbar"` مناسبًا.
- الأيقونات النصية لا تحتوي دائمًا على بدائل لقارئات الشاشة.
- لا إدارة focus عند الانتقال بين الخطوات أو فتح المودال.
- جرس الإشعارات بلا وظيفة واضحة.
- بعض aria labels ناقصة أو تأتي من مفاتيح ترجمة غير موجودة.
- المودال لا يحتوي focus trap أو دعم Escape.
- لا توجد رسائل خطأ مرتبطة بالحقول بشكل واضح.

---

## 11. الأداء وجودة الكود

- لا يوجد code splitting أو `React.lazy`.
- الخطوط محملة عبر Google Fonts `@import` مما قد يؤخر الرسم.
- توجد inline styles كثيرة في صفحات المشرف وHR.
- لا توجد PropTypes، وقاعدة `react/prop-types` معطلة.
- لا يوجد TypeScript.
- Context واسع وقد يسبب إعادة رسم مكونات كثيرة.
- timers غير منظفة في عدة أماكن.
- لا توجد Error Boundary.
- لا توجد حالات loading/error/retry للعمليات.
- لا يوجد test runner أو unit/integration/E2E tests.

### النتائج المعروفة للأدوات

- `npm run build`: ينجح.
- `npm run lint`: لا أخطاء، مع تحذيرات `react-refresh/only-export-components` في ملفات Context.
- `npm audit`: لا ثغرات معروفة حسب التقرير السابق.
- Prettier: التقرير السابق أشار إلى ملفات غير منسقة (`Attendance` و`dashboard.css`) ويجب إعادة التحقق.

---

## 12. الوثائق المتخلفة عن الكود

### `ARCHITECTURE.md`

ما زال يصف نسخة onboarding قديمة من المشروع، ولا يعكس:

- الداشبورد.
- المشرف وHR.
- العمليات والحضور.
- المكونات المشتركة الجديدة.
- `OnboardingContext` الحالي.
- مسارات المشروع الحالية.

### `README.md`

يحتاج تحديثًا لأنه:

- يذكر localStorage للتسجيل بينما التسجيل في الذاكرة فقط.
- يذكر RoleGate غير الموجود.
- لا يذكر معظم الصفحات الحالية.
- يصف بنية `LanguageContext` في مكان مختلف.
- يسرد TODOs قديمة أو غير دقيقة.

### `IMPLEMENTATION_PLAN.md`

يصف عناصر لم تُنفذ، مثل `RequestsContext` و`RoleGate` و`StatusBadge` و`ConfirmDialog`، ويذكر أن سؤال السيارة في VehicleBank بينما هو في PersonalInfo.

### `AUDIT_REPORT.md`

جزء من التقرير السابق أصبح قديمًا، خصوصًا ملاحظة أن `ROUTES.OPERATIONS` مفقود؛ الثابت موجود حاليًا. بقية المخاطر الأمنية والمعمارية ما زالت مهمة.

---

## 13. حالة Git والمخاطر المتعلقة بالتعديلات

وقت المراجعة كان هناك عدد من الملفات المعدلة وغير الملتزمة، منها:

- `src/constants/routes.js`
- `src/context/OnboardingContext.jsx`
- `src/i18n/copy.js`
- صفحات Documents وHR وOperations وPersonalInfo وSupervisor وUserProfile وVehicleBank
- `src/styles/onboarding.css`

وكانت هناك ملفات جديدة غير متعقبة، منها:

- `AUDIT_REPORT.md`
- `BACKEND_INTEGRATION_REVIEW.md`
- `src/components/shared/CourierApplicationInfo.jsx`
- `src/constants/supervisors.js`
- `src/utils/digits.js`

يجب قبل أي دمج:

- مراجعة `git diff` كاملًا.
- فصل التغييرات حسب الغرض.
- تشغيل build/lint/format.
- عدم الالتزام بأسرار أو بيانات شخصية.

---

## 14. توصيات مرتبة حسب الأولوية

### أولوية فورية

1. إلغاء وتدوير مفتاح Google API الموجود في تاريخ Git.
2. عدم استخدام بيانات الأشخاص الوهمية في أي بيئة إنتاج.
3. عدم اعتبار الواجهة حدًا أمنيًا قبل بناء Auth/RBAC في الخادم.
4. منع روابط السند غير الموثوقة.

### تحسينات Prototype قبل الباك إند

1. إصلاح مفاتيح i18n الناقصة.
2. نقل نصوص المشرف وHR والعمليات إلى `copy.js`.
3. إضافة route builders.
4. نقل Attendance إلى مسار واضح.
5. توجيه quick actions إلى Operations أو إزالة placeholders المتعارضة.
6. تنظيف timers في Login وOperations وContractViewer.
7. إصلاح حساب التاريخ المحلي للحضور.
8. جعل الداشبورد والبروفايل يعتمدان على الطلب الحالي بدل fallbacks مضللة.
9. فصل `saveSanad` عن `sendToAbsher`.
10. تصفية قوائم المشرف وHR حسب الحالة والصلاحية.
11. إضافة رسائل تحميل وخطأ وحالات empty.

### قبل ربط الباك إند

1. إنشاء `AuthProvider` وحراس مسارات.
2. إنشاء `src/services/http.js` موحد.
3. إنشاء خدمات auth/courier/applications/supervisor/hr/attendance/operations.
4. فصل Context الطلبات عن Context التسجيل.
5. نقل مصدر الحقيقة للطلبات والحالات إلى الخادم.
6. تنفيذ رفع ملفات حقيقي مع تحقق الحجم والنوع والملكية والفحص.
7. إضافة `.env.example` يحتوي فقط على إعدادات عامة مثل `VITE_API_BASE_URL`.
8. استخدام جلسة آمنة عبر HttpOnly/Secure/SameSite cookies عند دعم الباك إند.
9. تطبيق validation في الواجهة لتحسين UX وتكراره في الخادم كمرجع أساسي.
10. إضافة audit trail لقرارات المشرف وHR والعقد.

### الاختبارات المطلوبة

- Unit tests لـ utilities مثل `digits`, `fillTemplate`, وroute builders.
- Tests للـ Context وانتقالات الحالات.
- Tests لنماذج PersonalInfo وVehicleBank وDocuments.
- Integration test للتدفق الكامل.
- E2E test لتسجيل المندوب، موافقة المشرف، إجراء HR، وتوقيع العقد.
- Accessibility checks.

---

## 15. API مقترحة عند الربط

هذه مسارات مقترحة وليست موجودة حاليًا:

| العملية | Endpoint مقترح |
| --- | --- |
| تسجيل الدخول | `POST /auth/login` |
| الجلسة الحالية | `GET /auth/me` |
| تسجيل الخروج | `POST /auth/logout` |
| مسودة التسجيل | `PATCH /courier/onboarding-draft` |
| إنشاء طلب | `POST /courier/applications` |
| رفع مستند | `POST /courier/applications/:id/documents` |
| حالة الطلب الحالي | `GET /courier/applications/current` |
| طلبات المشرف | `GET /supervisor/applications` |
| قرار المشرف | `POST /supervisor/applications/:id/decision` |
| طلبات HR | `GET /hr/applications` |
| بيانات السند | `PUT /hr/applications/:id/sanad` |
| قرار أبشر | `POST /hr/applications/:id/absher-decision` |
| الحضور | `GET/POST /courier/attendance` |
| العمليات | `GET/POST /courier/operations-requests` |
| العقد | `GET/POST /courier/applications/:id/contract` |

يجب أن يفرض الخادم الصلاحيات وانتقالات الحالات والتحقق من الملكية والتعارض.

---

## 16. خلاصة نهائية

المشروع منظم ومناسب كنقطة بداية وPrototype، لكن الفجوة بين الواجهة الحالية ومتطلبات الإنتاج كبيرة.

أكبر المخاطر ليست في CSS أو تنظيم الملفات، بل في:

1. غياب المصادقة والصلاحيات.
2. وجود بيانات mock وPII داخل التطبيق.
3. عدم وجود API أو رفع ملفات حقيقي.
4. إمكانية تغيير حالات الطلبات من العميل.
5. روابط ومدخلات غير موثوقة.
6. تضارب الوثائق مع الكود الحالي.

**التوصية:** لا يتم نشر المشروع ببيانات فعلية قبل تنفيذ طبقة المصادقة والخدمات والصلاحيات والتحقق الخادمي، ثم تحديث الوثائق وإضافة اختبارات للتدفقات الأساسية.
