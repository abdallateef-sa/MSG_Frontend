# مراجعة جاهزية ربط الباك إند

> نطاق المراجعة: ملفات المصدر، الإعدادات، الواجهات، CSS، والوثائق الموجودة بالمشروع.  
> هذه مراجعة قراءة فقط؛ لا تتضمن تعديلات على كود التطبيق.

## الخلاصة

المشروع حاليًا واجهة Prototype جيدة لتدفق تسجيل المندوب وإدارة الطلبات، لكنه لا يحتوي على طبقة API أو مصادقة أو حفظ بيانات حقيقي. قبل تشغيله ببيانات فعلية، يجب نقل ملكية الهوية والصلاحيات والبيانات وتغيير الحالات إلى الباك إند.

## أولويات الربط

| الأولوية | المطلوب | الوضع الحالي |
| --- | --- | --- |
| حرجة | مصادقة حقيقية، جلسة مستخدم، وصلاحيات حسب الدور | تسجيل الدخول يختار الدور محليًا ثم يتنقل بتايمر. |
| حرجة | استبدال حالة الطلبات المحلية بطلبات API | `OnboardingContext` يحتفظ بالطلبات ويغيّر حالاتها في الذاكرة فقط. |
| حرجة | رفع المستندات فعليًا | الواجهة تسجل حالة الرفع كـ `true/false`، ولا ترفع ملفًا إلى خادم. |
| حرجة | الباك إند يملك انتقالات حالات الطلب | يمكن للواجهة حاليًا تغيير حالات المشرف وHR وأبشر والعقد محليًا. |
| مهمة | حماية Routes حسب الدور والجلسة | صفحات المندوب والمشرف وHR قابلة للوصول مباشرة. |
| مهمة | جلب القوائم التشغيلية من API | المشرفون والشركات والمخازن والبنوك والمدن وأنواع المركبات ثابتة محليًا. |
| مهمة | استبدال بيانات الداشبورد والبروفايل والعمليات | تعتمد على بيانات mock أو state محلي. |
| مهمة | نقل الحضور من `localStorage` إلى API | سجل الحضور محفوظ داخل المتصفح فقط. |
| مهمة | العقد والموافقة عليه من الباك إند | نص العقد والتوقيع الحاليان محليان. |

## العمليات والـ API المقترحة

| العملية | API مقترح | ملاحظات |
| --- | --- | --- |
| الدخول | `POST /auth/login` | يعيد هوية المستخدم والدور عبر جلسة آمنة، ويفضل Cookie من نوع `HttpOnly`. |
| الجلسة والخروج | `GET /auth/me`، `POST /auth/logout` | استعادة الجلسة عند تحميل التطبيق والتعامل مع انتهاء الصلاحية. |
| حفظ مسودة التسجيل | `PATCH /courier/onboarding-draft` | لحفظ كل خطوة من التسجيل وإرجاع أخطاء التحقق من الخادم. |
| إرسال طلب التسجيل | `POST /courier/applications` | يرسل بيانات المندوب والمركبة والبنك ومعرفات المستندات. |
| رفع مستند | `POST /courier/applications/:id/documents` | `multipart/form-data` أو رابط رفع موقّع؛ يعيد معرف المستند وحالته. |
| حالة طلب المندوب | `GET /courier/applications/current` | يتضمن الحالة والمستندات وبيانات العقد إن وجدت. |
| المشرفون المتاحون | `GET /supervisors?city=...` | الخادم يتحقق من صلاحية اختيار المشرف وتوافره. |
| الشركات والمخازن | `GET /companies`، `GET /companies/:id/warehouses` | لا تعتمد الواجهة على القيم الثابتة المحلية. |
| طلبات المشرف | `GET /supervisor/applications` | قائمة محددة بحسب هوية المشرف، مع بحث وترقيم صفحات. |
| قرار المشرف | `POST /supervisor/applications/:id/decision` | اعتماد/رفض مع الشركة والمخزن، ويتحقق الخادم من الحالة الحالية. |
| طلبات HR | `GET /hr/applications` | قائمة حسب صلاحيات موظف الموارد البشرية. |
| بيانات السند | `POST /hr/applications/:id/sanad` | الخادم يتحقق من رقم السند والرابط والمصدر الموثوق. |
| قرار أبشر | `POST /hr/applications/:id/absher-decision` | لا تسمح الواجهة وحدها بتعديل حالة الطلب. |
| الحضور | `GET/POST /courier/attendance` | الخادم يحدد تاريخ العمل والمنطقة الزمنية والسياسة التشغيلية. |
| الطلبات التشغيلية | `GET/POST /courier/operations-requests` | قائمة مرقمة وطلب جديد مع حالة يحددها الخادم. |
| العقد | `GET /courier/applications/:id/contract`، `POST /courier/applications/:id/contract-acceptance` | يعرض نسخة عقد معتمدة ويسجل الموافقة وسجل التدقيق. |

## الملفات التي ستتغير عند الربط

| الملف/المنطقة | التعديل المطلوب |
| --- | --- |
| `src/pages/Login/index.jsx` | استبدال اختيار الدور المحلي و`setTimeout` بطلب دخول وإدارة جلسة ورسائل خطأ. |
| `src/App.jsx` و`src/main.jsx` | إضافة `AuthProvider` وRoute Guards للأدوار: مندوب، مشرف، وHR. |
| `src/context/OnboardingContext.jsx` | فصل state المحلي عن بيانات الخادم؛ استبدال mock requests و`updateRequest` و`submitCourierApplication` بخدمات API. |
| `src/pages/PersonalInfo/index.jsx` | حفظ المسودة أو إرسالها إلى API؛ جلب المدن والمشرفين المتاحين من الخادم. |
| `src/pages/VehicleBank/index.jsx` | جلب البنوك وأنواع المركبات من API والتحقق النهائي على الخادم. |
| `src/pages/Documents/index.jsx` | رفع الملفات، إظهار التقدم/الفشل، وتخزين معرفات المستندات بدل boolean. |
| `src/constants/companies.js` و`src/constants/supervisors.js` | استبدال بيانات mock بمصادر API أو cache للبيانات المرجعية. |
| `src/pages/SupervisorRequests` و`src/pages/SupervisorRequestDetail` | جلب قائمة المشرف الخاصة به وإرسال القرار بعملية محمية. |
| `src/pages/HrRequests` و`src/pages/HrRequestDetail` | جلب قائمة HR وحفظ السند وقرار أبشر عبر API. |
| `src/pages/CourierStatus` و`ContractViewer` | جلب الحالة والعقد من الخادم وإرسال الموافقة بسجل تدقيق. |
| `src/pages/CourierDashboard` و`src/pages/UserProfile` | استبدال `mockDashboard` والـfallbacks ببيانات المستخدم الحالي. |
| `src/pages/Attendance` | استبدال `localStorage` بواجهة الحضور وربط المنطقة الزمنية بسياسة الباك إند. |
| `src/pages/Operations` | استبدال البيانات المحلية بقراءة وكتابة الطلبات التشغيلية من API. |

## متطلبات الأمان والبيانات

- لا تعتمد على الدور أو الحالة أو معرف المشرف أو الشركة القادمة من المتصفح؛ الباك إند يتحقق منها في كل طلب.
- استخدم جلسة آمنة عبر `HttpOnly` و`Secure` و`SameSite` cookie متى ما كان ذلك متاحًا، ولا تحفظ Token حساسًا في `localStorage`.
- طبق التحقق في الخادم على الهوية/الإقامة، رقم الجواز، الهاتف، الآيبان، اللوحة، التاريخ، والملفات؛ تحقق الواجهة لتحسين التجربة فقط.
- تحقق من نوع وحجم ومحتوى الملفات، وملكية الطلب، وفحص البرمجيات الخبيثة، وسياسة الاحتفاظ بالمستندات.
- الآيبان والجواز وبيانات الهوية حساسة: أخفِها جزئيًا في القوائم ولا تعرضها إلا للأدوار المخولة.
- رابط السند يجب أن يكون `https` ومن نطاقات معتمدة فقط؛ لا تفتح أي رابط يرسله المستخدم دون تحقق.
- استخدم حالة طلب مركزية على الخادم مع قواعد انتقال واضحة وتحقق من التعارض عند تعديل نفس الطلب.

## بنية Frontend مقترحة

إضافة طبقة خدمات مستقلة بدل استدعاءات موزعة داخل الصفحات:

```text
src/
  services/
    http.js
    auth.js
    applications.js
    documents.js
    supervisors.js
    companies.js
    attendance.js
    operations.js
  context/
    AuthContext.jsx
    OnboardingContext.jsx
    ApplicationContext.jsx
```

- `http.js`: عنوان API من `VITE_API_BASE_URL`، headers، JSON parsing، مهلة، إلغاء الطلب، وتنسيق أخطاء موحد.
- استخدم متغيرات `VITE_*` للقيم العامة فقط؛ لا تضع أسرارًا ضمنها لأن Vite يضمّنها في المتصفح.
- أبقِ state المحلي فقط للواجهة مثل فتح modal، الإدخال قبل الحفظ، والفلاتر؛ أما البيانات المشتركة فمصدرها API.
- أضف حالات `loading` و`error` و`empty` و`retry` لكل شاشة تعتمد على خادم.

## تحسينات مقترحة لاحقًا

- تقسيم `OnboardingContext` إلى سياقات أصغر: Auth، التسجيل، والطلبات.
- اعتماد TypeScript أو schema validation لتوحيد أشكال request/response وتقليل أخطاء الربط.
- استبدال `alert` و`confirm` بنوافذ تأكيد موحدة وقابلة للترجمة.
- إكمال مفاتيح الترجمة بدل النصوص العربية/الإنجليزية الثابتة في صفحات المشرف وHR.
- استبدال المسارات النصية التي تحتوي IDs بدوال Route Builders.
- إزالة تكرار عنوان قسم الطلبات الإدارية في `src/pages/Operations/index.jsx`.
- إصلاح تنبيهات Fast Refresh عبر فصل hook/context أو ضبط قاعدة ESLint.
- تشغيل Prettier على الملفات التي أظهرها الفحص بعد اعتماد التنسيق المطلوب.

## نتائج التحقق

- `npm run build`: ناجح.
- `npm run lint`: لا توجد أخطاء؛ يوجد تحذيران غير حاجزين متعلقان بـ React Fast Refresh في `OnboardingContext.jsx` و`LanguageContext.jsx`.
- `npx prettier --check .`: وجد تنسيقًا يحتاج Prettier في ستة ملفات، ولم يتم تنسيقها احترامًا لشرط عدم تغيير الكود.
- `git diff --check`: ناجح؛ لا توجد مشكلات whitespace في التعديلات الحالية.
